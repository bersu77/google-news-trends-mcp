# Required for JWT validation
MCP_JWT_ISSUER=https://issuer.example.com/

# Optional (default:  google trends)
MCP_JWT_AUDIENCE=clickup-mcp

# Provide exactly one of these signing key sources
MCP_JWT_JWKS_URL=https://issuer.example.com/.well-known/jwks.json
# MCP_JWT_PUBLIC_KEY="-----BEGIN PUBLIC KEY-----\n...base64...\n-----END PUBLIC KEY-----"

# Optional scope requirement
# MCP_JWT_REQUIRED_SCOPE=google-news.read

# Optional algorithms (comma-separated)
MCP_JWT_ALGORITHMS=RS256

# Server port (Docker uses this)
PORT=8080
